package com.fasterxml.jackson.databind.cfg;

final class ConfigOverride$Empty extends ConfigOverride {
   static final ConfigOverride$Empty INSTANCE = new ConfigOverride$Empty();

   private ConfigOverride$Empty() {
      super();
   }
}
